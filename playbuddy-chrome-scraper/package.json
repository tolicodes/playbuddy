{
  "name": "playbuddy-chrome-scraper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "webstore:upload": "chrome-webstore-upload upload",
    "webstore:publish": "chrome-webstore-upload publish",
    "webstore:deploy": "npm run build && chrome-webstore-upload",
    "webstore:zip": "npm run build && cd dist && zip -r ../playbuddy-chrome-scraper.zip ."
  },
  "webExt": {
    "sourceDir": "dist"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.2",
    "@types/chrome": "^0.0.332",
    "@types/turndown": "^5.0.5",
    "chrome-webstore-upload-cli": "^3.5.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.3"
  },
  "dependencies": {
    "moment": "^2.30.1",
    "moment-timezone": "^0.6.0",
    "turndown": "^7.2.0",
    "vite-plugin-chrome-extension": "^0.0.7"
  }
}
